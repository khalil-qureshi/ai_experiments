spring:
  datasource:
    url: jdbc:h2:mem:userdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  h2:
    console:
      enabled: true

app:
  security:
    jwt:
      secret: "wVy2a0m9r5u8x/A?D(G+KbPeShVmYq3t6w9z$C&F)J@NcRfUjXnZr4u7x!A%D*G"
      expiration-minutes: 120

logging:
  level:
    org.springframework.security: INFO
    org.hibernate.SQL: DEBUG
